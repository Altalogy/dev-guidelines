# 5. Build features with AI

**Goal:** a repeatable workflow for adding a feature with Claude Code.

## To be filled in

Topics to cover:

- **Spec first** — link back to [04-design-handoff.md](04-design-handoff.md)
- **Plan mode vs direct edits** — when each fits
- **Subagents** — when `Explore`, `Plan`, `general-purpose` actually help vs add overhead
- **Tests** — alongside vs after; what level of coverage we expect
- **Mid-flow review** — when to ask for `/review` before finishing
- **Close-the-loop** — updating `AGENTS.md` after non-trivial features so the next AI session sees them

## Open questions

- Do we want a "feature checklist" template (PR description, spec link, screenshots, tests)?
- How heavy-handed should `AGENTS.md` updates be — every feature, or only structural ones?

Next: [06-review.md](06-review.md).
